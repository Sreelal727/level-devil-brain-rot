// ============================================
// ShufflingButtonsTroll - Answer buttons shuffle during question
// (Logic handled in QuestionScene, this is just a marker class)
// ============================================

class ShufflingButtonsTroll extends BaseTroll {
    constructor(scene, config) {
        super(scene, config);
    }

    init() {
        this.activate();
    }
}
